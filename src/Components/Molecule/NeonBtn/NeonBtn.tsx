import * as React from "react";


const NeonBtn = () => {

    return (
            <div onClick={() => console.log("connect wallet")} className={"neon-button"}>Connect Wallet</div>
    )
};

export default NeonBtn;